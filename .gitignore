# The repo root is a flat mirror of the Apps Script project: every .gs and .html
# here is one file in the script editor. Nothing below is part of it — it is
# what npm leaves behind.
node_modules/
package.json
package-lock.json
